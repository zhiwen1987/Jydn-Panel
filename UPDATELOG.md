# 更新说明
> 老用户版本升级需要看升级说明，并且一定提前备份好重要数据。新用户可以直接使用最新版本。

## 当前测试版（未发布）
> 2026-08-04：当前内容只在 8008 测试环境验证，GitHub 与 `10.88.0.251` 同步暂停。
- 项目品牌统一为 Jydn-Panel，默认端口调整为 8008。
- 新增原生 Linux/systemd 安装器，保留配置、数据库、上传文件和运行数据。
- 站点 Logo 成为首页 Logo 与浏览器 Favicon 的统一来源。
- Powered By 改为后台风格设置中的安全 HTML 代码编辑。
- 调整系统应用菜单顺序，“关于”固定在最下面。
- 修复后台新增账号失败，并补充账号长度、角色和重复账号校验。
- 新增左侧分组目录固定名称和尺寸控制。
- 增强导入导出图片携带、恢复和缺失图标探测。
- 新增系统应用内的 Docker 插件管理器及容器、镜像、卷、网络管理 API/UI。
- 修复插件/Docker 页面破坏系统应用右侧框架和返回后功能失效的问题。
- 修复站点 Logo 上传无提示、浏览器标签图标未按配置更新的问题。
- 新增私有 GitHub 仓库 Release 版本检查，Token 由 systemd 环境文件注入。
- 详细内容见 [Jydn-Panel 定制变更说明](./doc/JYDN_CHANGELOG.md)。
- 代码与数据目录见 [项目文件目录架构](./doc/PROJECT_STRUCTURE.md)。
> 支持 Docker 无损升级，保留 `/data` 即可。
- 更新 About 页 OPENDOOR 入口到 `https://ai.opendoor.sbs`。
> 支持 Docker 无损升级，保留 `/data` 即可。
- 修复升级后普通浏览器可能继续命中旧前端缓存的问题。
- 增强首页和清缓存脚本的服务端缓存清理响应头。
> 支持 Docker 无损升级，保留 `/data` 即可。
- 修复暗黑主题下登录页内容辨识度不足的问题。
- 优化桌面端长按拖拽排序逻辑，避免平板和手机滚动时误触排序。
- 移除无用的系统状态栏组件显示。
- 修正图标区域左右间距不一致的问题。
- 修复暗黑主题下系统应用按钮文字不清晰的问题。
- 调亮关于页 SUPERDOOR 和 OPENDOOR 按钮在暗黑模式下的文字颜色。
> 支持 Docker 无损升级，保留 `/data` 即可。
- 修复更新后普通浏览器窗口仍显示旧前端的问题。
- 增加旧 Service Worker 和 Cache Storage 自动清理逻辑。
- 为 3005 前端入口和静态资源增加防旧缓存处理。
> 支持 Docker 无损升级，保留 `/data` 即可。
- 保留 3005 版前端、初始化数据和设置参数。
- Docker 默认端口统一为 3005。
> 支持上个版本直接升级无需特殊处理
- [新增] 增加分组，拖拽排序
- [新增] 搜索框
- [新增] 应用图标支持URL外链
- [新增] 图标支持纯透明
- [新增] 壁纸背景增加遮罩设置
- [新增] 右键菜单新增打开局域网或者互联网地址
- [优化] 网址输入框增加https/http提示
- [优化] 小图标模式，鼠标悬浮显示详情，支持隐藏图标标题
- [优化] 详情图标样式，支持隐藏描述信息
- [优化] 添加用户密码时限制字符
- [其他] 新增arm版本docker支持。[DockerHub](https://hub.docker.com/r/hslr/sun-panel)直接拉取即可
- [其他] 新增多平台二进制文件运行。[Releases](https://github.com/hslr-s/sun-panel/releases)
> 支持从 v0.1.19 直接升级，无需特殊处理
- [发布] 增加 GitHub Release，便于新版本展示与下载
- [修正] 同步程序内版本号到 v0.1.20
- [验证] README 中 Docker 安装方式继续保持不变，镜像仍使用 `ghcr.io/zhiwen1987/jydn-panel:latest`
